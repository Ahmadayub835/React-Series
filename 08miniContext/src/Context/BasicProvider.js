import React from 'react'
import { createContext } from 'react'

    export const myContext = createContext()

    state = {
        name: 'Ahmad',
        lastname : 'Ayub',
    }
  return (
    <div>BasicProvider</div>
  )

